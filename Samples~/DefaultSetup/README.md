# Default Setup Sample

This sample shows a minimal bootstrap for NativeSocial integration.

1. Open `NativeSocialBootstrap.cs` and configure your achievement IDs.
2. Attach the component to any GameObject in your startup scene (or call from your existing bootstrapper).
3. Call `NativeSocial.Report()` whenever the player progresses on an achievement.
